@model FacultiesListViewModel
<p>
    <partial name="Btn-Create" />
</p>
<hr />
<table class="table">
    <thead>
        <tr>
            <th>Tên khoa</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model?.Faculties ?? Enumerable.Empty<Faculty>())
        {
            <tr>
                <td>@item.Ten</td>
                <td><partial name="IndexBtn" model="item" /></td>
            </tr>
        }
    </tbody>
</table>
<partial name="PagingBtn" />
