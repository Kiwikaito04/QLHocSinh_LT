@model TeachersListViewModel
<h2>Danh sách giáo viên</h2>
<p>
    <partial name="Btn-Create" />
</p>
<hr />
@if (!string.IsNullOrEmpty(ViewBag.Message))
{
    <div class="alert alert-success">
        @ViewBag.Message
    </div>
}
<table class="table">
    <thead>
        <tr>
            <th>Username</th>
            <th>Họ tên</th>
            <th>Giới tính</th>
            <th>Ngày sinh</th>
            <th>Số điện thoại</th>
            <th>Khoa</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model?.Teachers ?? Enumerable.Empty<Teacher>())
        {
            <tr>
                <td>
                    @item.IdentityUser.UserName
                </td>
                <td>
                    @item.HoTen
                </td>
                <td>
                    @item.GioiTinh
                </td>
                <td>
                    @item.NgaySinh.ToShortDateString()
                </td>
                <td>
                    @item.SDT
                </td>
                <td>
                    @item.Faculty.Ten
                </td>
                <td>
                    <partial name="IndexBtn" model="item" />
                </td>
            </tr>
        }
    </tbody>
</table>
<partial name="PagingBtn" />
